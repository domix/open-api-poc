get:
  description: Gets data
  summary: Gets data
  operationId: ope
  tags:
    - public
  responses:
    "200":
      description: Gets data
      content:
        application/json:
          schema:
            $ref: '../schemas/Foo.yaml'
